/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2025_11_15.models.components;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * PayrollBatchStatus
 * 
 * <p>The lifecycle status of the batch request itself. Terminal values are `completed` (processing
 * finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch
 * crashed at the system level; can be retried). This is distinct from the per-payroll `status`
 * returned inside `results[]`.
 * 
 * <p>A `completed` batch does not imply every payroll was cancelled.
 */
public enum PayrollBatchStatus {
    PENDING("pending"),
    PROCESSING("processing"),
    COMPLETED("completed"),
    FAILED("failed");

    @JsonValue
    private final String value;

    PayrollBatchStatus(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<PayrollBatchStatus> fromValue(String value) {
        for (PayrollBatchStatus o: PayrollBatchStatus.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

