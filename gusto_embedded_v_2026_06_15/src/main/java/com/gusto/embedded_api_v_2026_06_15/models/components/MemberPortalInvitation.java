/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2026_06_15.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api_v_2026_06_15.utils.Utils;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;

/**
 * MemberPortalInvitation
 * 
 * <p>The status of a member portal invitation for an employee or contractor. Returned by `GET
 * /v1/employees/{employee_id}/member_portal_invitations` and `GET
 * /v1/contractors/{contractor_uuid}/member_portal_invitations`.
 */
public class MemberPortalInvitation {
    /**
     * Current status of the member portal invitation.
     * - `pending`: The invitation record has been created but the invitation email has not yet been
     * delivered.
     * - `sent`: The invitation email has been delivered to the member.
     * - `verified`: The member has been verified by the member portal partner.
     * - `complete`: The member has fully completed their member portal registration.
     * - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it
     * was superseded.
     */
    @JsonProperty("status")
    private MemberPortalInvitationStatus1 status;

    /**
     * Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation
     * token has expired or when the invitation has been `cancelled`. To reissue an expired invitation,
     * call `POST` on this endpoint again — a new token will be generated and will override the prior one.
     */
    @JsonProperty("expired")
    private boolean expired;

    @JsonCreator
    public MemberPortalInvitation(
            @JsonProperty("status") MemberPortalInvitationStatus1 status,
            @JsonProperty("expired") boolean expired) {
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(expired, "expired");
        this.status = status;
        this.expired = expired;
    }

    /**
     * Current status of the member portal invitation.
     * - `pending`: The invitation record has been created but the invitation email has not yet been
     * delivered.
     * - `sent`: The invitation email has been delivered to the member.
     * - `verified`: The member has been verified by the member portal partner.
     * - `complete`: The member has fully completed their member portal registration.
     * - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it
     * was superseded.
     */
    @JsonIgnore
    public MemberPortalInvitationStatus1 status() {
        return status;
    }

    /**
     * Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation
     * token has expired or when the invitation has been `cancelled`. To reissue an expired invitation,
     * call `POST` on this endpoint again — a new token will be generated and will override the prior one.
     */
    @JsonIgnore
    public boolean expired() {
        return expired;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Current status of the member portal invitation.
     * - `pending`: The invitation record has been created but the invitation email has not yet been
     * delivered.
     * - `sent`: The invitation email has been delivered to the member.
     * - `verified`: The member has been verified by the member portal partner.
     * - `complete`: The member has fully completed their member portal registration.
     * - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it
     * was superseded.
     */
    public MemberPortalInvitation withStatus(MemberPortalInvitationStatus1 status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    /**
     * Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation
     * token has expired or when the invitation has been `cancelled`. To reissue an expired invitation,
     * call `POST` on this endpoint again — a new token will be generated and will override the prior one.
     */
    public MemberPortalInvitation withExpired(boolean expired) {
        Utils.checkNotNull(expired, "expired");
        this.expired = expired;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        MemberPortalInvitation other = (MemberPortalInvitation) o;
        return 
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.expired, other.expired);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            status, expired);
    }
    
    @Override
    public String toString() {
        return Utils.toString(MemberPortalInvitation.class,
                "status", status,
                "expired", expired);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private MemberPortalInvitationStatus1 status;

        private Boolean expired;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Current status of the member portal invitation.
         * - `pending`: The invitation record has been created but the invitation email has not yet been
         * delivered.
         * - `sent`: The invitation email has been delivered to the member.
         * - `verified`: The member has been verified by the member portal partner.
         * - `complete`: The member has fully completed their member portal registration.
         * - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it
         * was superseded.
         */
        public Builder status(MemberPortalInvitationStatus1 status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        /**
         * Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation
         * token has expired or when the invitation has been `cancelled`. To reissue an expired invitation,
         * call `POST` on this endpoint again — a new token will be generated and will override the prior one.
         */
        public Builder expired(boolean expired) {
            Utils.checkNotNull(expired, "expired");
            this.expired = expired;
            return this;
        }

        public MemberPortalInvitation build() {

            return new MemberPortalInvitation(
                status, expired);
        }

    }
}
