/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2026_06_15.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api_v_2026_06_15.utils.Utils;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * PayrollBatchResults
 * 
 * <p>A payroll cancellation batch with per-payroll results.
 */
public class PayrollBatchResults {
    /**
     * The unique identifier of the payroll cancellation batch.
     */
    @JsonProperty("uuid")
    private String uuid;

    /**
     * The idempotency key provided when creating the batch.
     */
    @JsonProperty("idempotency_key")
    private String idempotencyKey;

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch
     * crashed at the system level; can be retried). This is distinct from the per-payroll `status`
     * returned inside `results[]`.
     * 
     * <p>A `completed` batch does not imply every payroll was cancelled.
     */
    @JsonProperty("status")
    private PayrollBatchResultsStatus status;

    /**
     * The timestamp when the batch was submitted.
     */
    @JsonProperty("submitted_at")
    private OffsetDateTime submittedAt;

    /**
     * The timestamp when the batch processing completed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("completed_at")
    private JsonNullable<OffsetDateTime> completedAt;

    /**
     * The number of payrolls submitted in the batch.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("submitted_items")
    private JsonNullable<Long> submittedItems;

    /**
     * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
     * terminal status.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("processed_items")
    private Optional<Long> processedItems;

    /**
     * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("excluded_items")
    private Optional<Long> excludedItems;

    /**
     * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
     * per authorized payroll.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("results")
    private Optional<? extends List<Results>> results;

    /**
     * Payrolls that could not be processed, determined at submission time. Only present once the batch
     * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
     * `results` or `exclusions`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("exclusions")
    private Optional<? extends List<Exclusions>> exclusions;

    @JsonCreator
    public PayrollBatchResults(
            @JsonProperty("uuid") String uuid,
            @JsonProperty("idempotency_key") String idempotencyKey,
            @JsonProperty("status") PayrollBatchResultsStatus status,
            @JsonProperty("submitted_at") OffsetDateTime submittedAt,
            @JsonProperty("completed_at") JsonNullable<OffsetDateTime> completedAt,
            @JsonProperty("submitted_items") JsonNullable<Long> submittedItems,
            @JsonProperty("processed_items") Optional<Long> processedItems,
            @JsonProperty("excluded_items") Optional<Long> excludedItems,
            @JsonProperty("results") Optional<? extends List<Results>> results,
            @JsonProperty("exclusions") Optional<? extends List<Exclusions>> exclusions) {
        Utils.checkNotNull(uuid, "uuid");
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(submittedAt, "submittedAt");
        Utils.checkNotNull(completedAt, "completedAt");
        Utils.checkNotNull(submittedItems, "submittedItems");
        Utils.checkNotNull(processedItems, "processedItems");
        Utils.checkNotNull(excludedItems, "excludedItems");
        Utils.checkNotNull(results, "results");
        Utils.checkNotNull(exclusions, "exclusions");
        this.uuid = uuid;
        this.idempotencyKey = idempotencyKey;
        this.status = status;
        this.submittedAt = submittedAt;
        this.completedAt = completedAt;
        this.submittedItems = submittedItems;
        this.processedItems = processedItems;
        this.excludedItems = excludedItems;
        this.results = results;
        this.exclusions = exclusions;
    }
    
    public PayrollBatchResults(
            String uuid,
            String idempotencyKey,
            PayrollBatchResultsStatus status,
            OffsetDateTime submittedAt) {
        this(uuid, idempotencyKey, status,
            submittedAt, JsonNullable.undefined(), JsonNullable.undefined(),
            Optional.empty(), Optional.empty(), Optional.empty(),
            Optional.empty());
    }

    /**
     * The unique identifier of the payroll cancellation batch.
     */
    @JsonIgnore
    public String uuid() {
        return uuid;
    }

    /**
     * The idempotency key provided when creating the batch.
     */
    @JsonIgnore
    public String idempotencyKey() {
        return idempotencyKey;
    }

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch
     * crashed at the system level; can be retried). This is distinct from the per-payroll `status`
     * returned inside `results[]`.
     * 
     * <p>A `completed` batch does not imply every payroll was cancelled.
     */
    @JsonIgnore
    public PayrollBatchResultsStatus status() {
        return status;
    }

    /**
     * The timestamp when the batch was submitted.
     */
    @JsonIgnore
    public OffsetDateTime submittedAt() {
        return submittedAt;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    @JsonIgnore
    public JsonNullable<OffsetDateTime> completedAt() {
        return completedAt;
    }

    /**
     * The number of payrolls submitted in the batch.
     */
    @JsonIgnore
    public JsonNullable<Long> submittedItems() {
        return submittedItems;
    }

    /**
     * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
     * terminal status.
     */
    @JsonIgnore
    public Optional<Long> processedItems() {
        return processedItems;
    }

    /**
     * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    @JsonIgnore
    public Optional<Long> excludedItems() {
        return excludedItems;
    }

    /**
     * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
     * per authorized payroll.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<Results>> results() {
        return (Optional<List<Results>>) results;
    }

    /**
     * Payrolls that could not be processed, determined at submission time. Only present once the batch
     * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
     * `results` or `exclusions`.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<Exclusions>> exclusions() {
        return (Optional<List<Exclusions>>) exclusions;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The unique identifier of the payroll cancellation batch.
     */
    public PayrollBatchResults withUuid(String uuid) {
        Utils.checkNotNull(uuid, "uuid");
        this.uuid = uuid;
        return this;
    }

    /**
     * The idempotency key provided when creating the batch.
     */
    public PayrollBatchResults withIdempotencyKey(String idempotencyKey) {
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        this.idempotencyKey = idempotencyKey;
        return this;
    }

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch
     * crashed at the system level; can be retried). This is distinct from the per-payroll `status`
     * returned inside `results[]`.
     * 
     * <p>A `completed` batch does not imply every payroll was cancelled.
     */
    public PayrollBatchResults withStatus(PayrollBatchResultsStatus status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    /**
     * The timestamp when the batch was submitted.
     */
    public PayrollBatchResults withSubmittedAt(OffsetDateTime submittedAt) {
        Utils.checkNotNull(submittedAt, "submittedAt");
        this.submittedAt = submittedAt;
        return this;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    public PayrollBatchResults withCompletedAt(OffsetDateTime completedAt) {
        Utils.checkNotNull(completedAt, "completedAt");
        this.completedAt = JsonNullable.of(completedAt);
        return this;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    public PayrollBatchResults withCompletedAt(JsonNullable<OffsetDateTime> completedAt) {
        Utils.checkNotNull(completedAt, "completedAt");
        this.completedAt = completedAt;
        return this;
    }

    /**
     * The number of payrolls submitted in the batch.
     */
    public PayrollBatchResults withSubmittedItems(long submittedItems) {
        Utils.checkNotNull(submittedItems, "submittedItems");
        this.submittedItems = JsonNullable.of(submittedItems);
        return this;
    }

    /**
     * The number of payrolls submitted in the batch.
     */
    public PayrollBatchResults withSubmittedItems(JsonNullable<Long> submittedItems) {
        Utils.checkNotNull(submittedItems, "submittedItems");
        this.submittedItems = submittedItems;
        return this;
    }

    /**
     * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
     * terminal status.
     */
    public PayrollBatchResults withProcessedItems(long processedItems) {
        Utils.checkNotNull(processedItems, "processedItems");
        this.processedItems = Optional.ofNullable(processedItems);
        return this;
    }


    /**
     * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
     * terminal status.
     */
    public PayrollBatchResults withProcessedItems(Optional<Long> processedItems) {
        Utils.checkNotNull(processedItems, "processedItems");
        this.processedItems = processedItems;
        return this;
    }

    /**
     * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollBatchResults withExcludedItems(long excludedItems) {
        Utils.checkNotNull(excludedItems, "excludedItems");
        this.excludedItems = Optional.ofNullable(excludedItems);
        return this;
    }


    /**
     * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollBatchResults withExcludedItems(Optional<Long> excludedItems) {
        Utils.checkNotNull(excludedItems, "excludedItems");
        this.excludedItems = excludedItems;
        return this;
    }

    /**
     * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
     * per authorized payroll.
     */
    public PayrollBatchResults withResults(List<Results> results) {
        Utils.checkNotNull(results, "results");
        this.results = Optional.ofNullable(results);
        return this;
    }


    /**
     * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
     * per authorized payroll.
     */
    public PayrollBatchResults withResults(Optional<? extends List<Results>> results) {
        Utils.checkNotNull(results, "results");
        this.results = results;
        return this;
    }

    /**
     * Payrolls that could not be processed, determined at submission time. Only present once the batch
     * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
     * `results` or `exclusions`.
     */
    public PayrollBatchResults withExclusions(List<Exclusions> exclusions) {
        Utils.checkNotNull(exclusions, "exclusions");
        this.exclusions = Optional.ofNullable(exclusions);
        return this;
    }


    /**
     * Payrolls that could not be processed, determined at submission time. Only present once the batch
     * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
     * `results` or `exclusions`.
     */
    public PayrollBatchResults withExclusions(Optional<? extends List<Exclusions>> exclusions) {
        Utils.checkNotNull(exclusions, "exclusions");
        this.exclusions = exclusions;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PayrollBatchResults other = (PayrollBatchResults) o;
        return 
            Utils.enhancedDeepEquals(this.uuid, other.uuid) &&
            Utils.enhancedDeepEquals(this.idempotencyKey, other.idempotencyKey) &&
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.submittedAt, other.submittedAt) &&
            Utils.enhancedDeepEquals(this.completedAt, other.completedAt) &&
            Utils.enhancedDeepEquals(this.submittedItems, other.submittedItems) &&
            Utils.enhancedDeepEquals(this.processedItems, other.processedItems) &&
            Utils.enhancedDeepEquals(this.excludedItems, other.excludedItems) &&
            Utils.enhancedDeepEquals(this.results, other.results) &&
            Utils.enhancedDeepEquals(this.exclusions, other.exclusions);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            uuid, idempotencyKey, status,
            submittedAt, completedAt, submittedItems,
            processedItems, excludedItems, results,
            exclusions);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PayrollBatchResults.class,
                "uuid", uuid,
                "idempotencyKey", idempotencyKey,
                "status", status,
                "submittedAt", submittedAt,
                "completedAt", completedAt,
                "submittedItems", submittedItems,
                "processedItems", processedItems,
                "excludedItems", excludedItems,
                "results", results,
                "exclusions", exclusions);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String uuid;

        private String idempotencyKey;

        private PayrollBatchResultsStatus status;

        private OffsetDateTime submittedAt;

        private JsonNullable<OffsetDateTime> completedAt = JsonNullable.undefined();

        private JsonNullable<Long> submittedItems = JsonNullable.undefined();

        private Optional<Long> processedItems = Optional.empty();

        private Optional<Long> excludedItems = Optional.empty();

        private Optional<? extends List<Results>> results = Optional.empty();

        private Optional<? extends List<Exclusions>> exclusions = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The unique identifier of the payroll cancellation batch.
         */
        public Builder uuid(String uuid) {
            Utils.checkNotNull(uuid, "uuid");
            this.uuid = uuid;
            return this;
        }


        /**
         * The idempotency key provided when creating the batch.
         */
        public Builder idempotencyKey(String idempotencyKey) {
            Utils.checkNotNull(idempotencyKey, "idempotencyKey");
            this.idempotencyKey = idempotencyKey;
            return this;
        }


        /**
         * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
         * finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch
         * crashed at the system level; can be retried). This is distinct from the per-payroll `status`
         * returned inside `results[]`.
         * 
         * <p>A `completed` batch does not imply every payroll was cancelled.
         */
        public Builder status(PayrollBatchResultsStatus status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        /**
         * The timestamp when the batch was submitted.
         */
        public Builder submittedAt(OffsetDateTime submittedAt) {
            Utils.checkNotNull(submittedAt, "submittedAt");
            this.submittedAt = submittedAt;
            return this;
        }


        /**
         * The timestamp when the batch processing completed.
         */
        public Builder completedAt(OffsetDateTime completedAt) {
            Utils.checkNotNull(completedAt, "completedAt");
            this.completedAt = JsonNullable.of(completedAt);
            return this;
        }

        /**
         * The timestamp when the batch processing completed.
         */
        public Builder completedAt(JsonNullable<OffsetDateTime> completedAt) {
            Utils.checkNotNull(completedAt, "completedAt");
            this.completedAt = completedAt;
            return this;
        }


        /**
         * The number of payrolls submitted in the batch.
         */
        public Builder submittedItems(long submittedItems) {
            Utils.checkNotNull(submittedItems, "submittedItems");
            this.submittedItems = JsonNullable.of(submittedItems);
            return this;
        }

        /**
         * The number of payrolls submitted in the batch.
         */
        public Builder submittedItems(JsonNullable<Long> submittedItems) {
            Utils.checkNotNull(submittedItems, "submittedItems");
            this.submittedItems = submittedItems;
            return this;
        }


        /**
         * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
         * terminal status.
         */
        public Builder processedItems(long processedItems) {
            Utils.checkNotNull(processedItems, "processedItems");
            this.processedItems = Optional.ofNullable(processedItems);
            return this;
        }

        /**
         * The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a
         * terminal status.
         */
        public Builder processedItems(Optional<Long> processedItems) {
            Utils.checkNotNull(processedItems, "processedItems");
            this.processedItems = processedItems;
            return this;
        }


        /**
         * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
         * status.
         */
        public Builder excludedItems(long excludedItems) {
            Utils.checkNotNull(excludedItems, "excludedItems");
            this.excludedItems = Optional.ofNullable(excludedItems);
            return this;
        }

        /**
         * The number of payrolls excluded from processing. Only present once the batch reaches a terminal
         * status.
         */
        public Builder excludedItems(Optional<Long> excludedItems) {
            Utils.checkNotNull(excludedItems, "excludedItems");
            this.excludedItems = excludedItems;
            return this;
        }


        /**
         * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
         * per authorized payroll.
         */
        public Builder results(List<Results> results) {
            Utils.checkNotNull(results, "results");
            this.results = Optional.ofNullable(results);
            return this;
        }

        /**
         * Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry
         * per authorized payroll.
         */
        public Builder results(Optional<? extends List<Results>> results) {
            Utils.checkNotNull(results, "results");
            this.results = results;
            return this;
        }


        /**
         * Payrolls that could not be processed, determined at submission time. Only present once the batch
         * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
         * `results` or `exclusions`.
         */
        public Builder exclusions(List<Exclusions> exclusions) {
            Utils.checkNotNull(exclusions, "exclusions");
            this.exclusions = Optional.ofNullable(exclusions);
            return this;
        }

        /**
         * Payrolls that could not be processed, determined at submission time. Only present once the batch
         * reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of
         * `results` or `exclusions`.
         */
        public Builder exclusions(Optional<? extends List<Exclusions>> exclusions) {
            Utils.checkNotNull(exclusions, "exclusions");
            this.exclusions = exclusions;
            return this;
        }

        public PayrollBatchResults build() {

            return new PayrollBatchResults(
                uuid, idempotencyKey, status,
                submittedAt, completedAt, submittedItems,
                processedItems, excludedItems, results,
                exclusions);
        }

    }
}
