/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2026_06_15.models.components;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * ReverseWireTransactionStatus
 * 
 * <p>Current status of the reverse wire transaction.
 * - `pending_processing`: The reverse wire has been initiated and is awaiting processing by the
 * banking network.
 * - `processed`: The reverse wire was successfully settled and funds have been received.
 * - `rejected`: The reverse wire was rejected by the receiving bank (e.g. invalid account,
 * insufficient funds).
 * - `failed`: The reverse wire failed during processing due to a system or network error.
 */
public enum ReverseWireTransactionStatus {
    PENDING_PROCESSING("pending_processing"),
    PROCESSED("processed"),
    REJECTED("rejected"),
    FAILED("failed");

    @JsonValue
    private final String value;

    ReverseWireTransactionStatus(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<ReverseWireTransactionStatus> fromValue(String value) {
        for (ReverseWireTransactionStatus o: ReverseWireTransactionStatus.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

