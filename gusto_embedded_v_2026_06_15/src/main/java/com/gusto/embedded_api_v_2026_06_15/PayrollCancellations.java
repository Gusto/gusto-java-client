/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2026_06_15;

import static com.gusto.embedded_api_v_2026_06_15.operations.Operations.RequestOperation;

import com.gusto.embedded_api_v_2026_06_15.models.operations.GetV1PayrollBatchesPayrollBatchUuidHeaderXGustoAPIVersion;
import com.gusto.embedded_api_v_2026_06_15.models.operations.GetV1PayrollBatchesPayrollBatchUuidRequest;
import com.gusto.embedded_api_v_2026_06_15.models.operations.GetV1PayrollBatchesPayrollBatchUuidRequestBuilder;
import com.gusto.embedded_api_v_2026_06_15.models.operations.GetV1PayrollBatchesPayrollBatchUuidResponse;
import com.gusto.embedded_api_v_2026_06_15.models.operations.GetV1PayrollBatchesPayrollBatchUuidSecurity;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesHeaderXGustoAPIVersion;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesRequest;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesRequestBody;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesRequestBuilder;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesResponse;
import com.gusto.embedded_api_v_2026_06_15.models.operations.PostV1PayrollBatchesSecurity;
import com.gusto.embedded_api_v_2026_06_15.operations.GetV1PayrollBatchesPayrollBatchUuid;
import com.gusto.embedded_api_v_2026_06_15.operations.PostV1PayrollBatches;
import com.gusto.embedded_api_v_2026_06_15.utils.Headers;
import java.lang.String;
import java.util.Optional;


public class PayrollCancellations {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncPayrollCancellations asyncSDK;

    PayrollCancellations(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncPayrollCancellations(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncPayrollCancellations async() {
        return asyncSDK;
    }

    /**
     * Create a payroll cancellation batch
     * 
     * <p>Cancels up to 100 payrolls across one or more companies the partner is mapped to, asynchronously.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_batches/{payroll_batch_uuid}` for status and per-payroll results.
     * 
     * <p>Each item carries the payroll `uuid` and the `company_uuid` that owns it. A payroll whose company is
     * not mapped to the partner — or that doesn't exist — is recorded as a `not_found` exclusion rather
     * than a hard error, so every requested UUID lands in either `results` or `exclusions`.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * processing occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:write`
     * 
     * @return The call builder
     */
    public PostV1PayrollBatchesRequestBuilder postV1PayrollBatches() {
        return new PostV1PayrollBatchesRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a payroll cancellation batch
     * 
     * <p>Cancels up to 100 payrolls across one or more companies the partner is mapped to, asynchronously.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_batches/{payroll_batch_uuid}` for status and per-payroll results.
     * 
     * <p>Each item carries the payroll `uuid` and the `company_uuid` that owns it. A payroll whose company is
     * not mapped to the partner — or that doesn't exist — is recorded as a `not_found` exclusion rather
     * than a hard error, so every requested UUID lands in either `results` or `exclusions`.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * processing occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:write`
     * 
     * @param security The security details to use for authentication.
     * @param requestBody 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PostV1PayrollBatchesResponse postV1PayrollBatches(PostV1PayrollBatchesSecurity security, PostV1PayrollBatchesRequestBody requestBody) {
        return postV1PayrollBatches(security, Optional.empty(), requestBody);
    }

    /**
     * Create a payroll cancellation batch
     * 
     * <p>Cancels up to 100 payrolls across one or more companies the partner is mapped to, asynchronously.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_batches/{payroll_batch_uuid}` for status and per-payroll results.
     * 
     * <p>Each item carries the payroll `uuid` and the `company_uuid` that owns it. A payroll whose company is
     * not mapped to the partner — or that doesn't exist — is recorded as a `not_found` exclusion rather
     * than a hard error, so every requested UUID lands in either `results` or `exclusions`.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * processing occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:write`
     * 
     * @param security The security details to use for authentication.
     * @param xGustoAPIVersion Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
     * @param requestBody 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PostV1PayrollBatchesResponse postV1PayrollBatches(
            PostV1PayrollBatchesSecurity security, Optional<? extends PostV1PayrollBatchesHeaderXGustoAPIVersion> xGustoAPIVersion,
            PostV1PayrollBatchesRequestBody requestBody) {
        PostV1PayrollBatchesRequest request =
            PostV1PayrollBatchesRequest
                .builder()
                .xGustoAPIVersion(xGustoAPIVersion)
                .requestBody(requestBody)
                .build();
        RequestOperation<PostV1PayrollBatchesRequest, PostV1PayrollBatchesResponse> operation
              = new PostV1PayrollBatches.Sync(sdkConfiguration, security, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get a payroll cancellation batch
     * 
     * <p>Returns the status and per-payroll results of a payroll cancellation batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the `results` array (one entry per authorized payroll, each with its
     * own per-payroll `status` — `success` or `failed`) and the `exclusions` array (one entry per payroll
     * that could not be processed). A cancel is atomic, so a per-payroll result is only ever `success` or
     * `failed` — never `partial_success`.
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is the
     * request lifecycle, distinct from the per-payroll `status` inside `results[]`. A `completed` batch
     * does not imply every payroll was cancelled — inspect the array for per-payroll outcomes.
     * 
     * <p>Results are stored in Redis with a limited TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:read`
     * 
     * @return The call builder
     */
    public GetV1PayrollBatchesPayrollBatchUuidRequestBuilder getV1PayrollBatchesPayrollBatchUuid() {
        return new GetV1PayrollBatchesPayrollBatchUuidRequestBuilder(sdkConfiguration);
    }

    /**
     * Get a payroll cancellation batch
     * 
     * <p>Returns the status and per-payroll results of a payroll cancellation batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the `results` array (one entry per authorized payroll, each with its
     * own per-payroll `status` — `success` or `failed`) and the `exclusions` array (one entry per payroll
     * that could not be processed). A cancel is atomic, so a per-payroll result is only ever `success` or
     * `failed` — never `partial_success`.
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is the
     * request lifecycle, distinct from the per-payroll `status` inside `results[]`. A `completed` batch
     * does not imply every payroll was cancelled — inspect the array for per-payroll outcomes.
     * 
     * <p>Results are stored in Redis with a limited TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:read`
     * 
     * @param security The security details to use for authentication.
     * @param payrollBatchUuid The UUID of the payroll cancellation batch returned by `POST /v1/payroll_batches`.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetV1PayrollBatchesPayrollBatchUuidResponse getV1PayrollBatchesPayrollBatchUuid(GetV1PayrollBatchesPayrollBatchUuidSecurity security, String payrollBatchUuid) {
        return getV1PayrollBatchesPayrollBatchUuid(security, Optional.empty(), payrollBatchUuid);
    }

    /**
     * Get a payroll cancellation batch
     * 
     * <p>Returns the status and per-payroll results of a payroll cancellation batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the `results` array (one entry per authorized payroll, each with its
     * own per-payroll `status` — `success` or `failed`) and the `exclusions` array (one entry per payroll
     * that could not be processed). A cancel is atomic, so a per-payroll result is only ever `success` or
     * `failed` — never `partial_success`.
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is the
     * request lifecycle, distinct from the per-payroll `status` inside `results[]`. A `completed` batch
     * does not imply every payroll was cancelled — inspect the array for per-payroll outcomes.
     * 
     * <p>Results are stored in Redis with a limited TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_batches:read`
     * 
     * @param security The security details to use for authentication.
     * @param xGustoAPIVersion Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
     * @param payrollBatchUuid The UUID of the payroll cancellation batch returned by `POST /v1/payroll_batches`.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetV1PayrollBatchesPayrollBatchUuidResponse getV1PayrollBatchesPayrollBatchUuid(
            GetV1PayrollBatchesPayrollBatchUuidSecurity security, Optional<? extends GetV1PayrollBatchesPayrollBatchUuidHeaderXGustoAPIVersion> xGustoAPIVersion,
            String payrollBatchUuid) {
        GetV1PayrollBatchesPayrollBatchUuidRequest request =
            GetV1PayrollBatchesPayrollBatchUuidRequest
                .builder()
                .xGustoAPIVersion(xGustoAPIVersion)
                .payrollBatchUuid(payrollBatchUuid)
                .build();
        RequestOperation<GetV1PayrollBatchesPayrollBatchUuidRequest, GetV1PayrollBatchesPayrollBatchUuidResponse> operation
              = new GetV1PayrollBatchesPayrollBatchUuid.Sync(sdkConfiguration, security, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
