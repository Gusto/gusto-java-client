/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */


package com.gusto.embedded_api.models.operations;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

public class RequirementSets {

    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("key")
    private Optional<String> key;

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("effective_from")
    private JsonNullable<String> effectiveFrom;

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("state")
    private Optional<String> state;

    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("requirements")
    private Optional<? extends List<Requirements>> requirements;

    @JsonCreator
    public RequirementSets(
            @JsonProperty("key") Optional<String> key,
            @JsonProperty("effective_from") JsonNullable<String> effectiveFrom,
            @JsonProperty("state") Optional<String> state,
            @JsonProperty("requirements") Optional<? extends List<Requirements>> requirements) {
        Utils.checkNotNull(key, "key");
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        Utils.checkNotNull(state, "state");
        Utils.checkNotNull(requirements, "requirements");
        this.key = key;
        this.effectiveFrom = effectiveFrom;
        this.state = state;
        this.requirements = requirements;
    }
    
    public RequirementSets() {
        this(Optional.empty(), JsonNullable.undefined(), Optional.empty(), Optional.empty());
    }

    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
     */
    @JsonIgnore
    public Optional<String> key() {
        return key;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
     */
    @JsonIgnore
    public JsonNullable<String> effectiveFrom() {
        return effectiveFrom;
    }

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
     */
    @JsonIgnore
    public Optional<String> state() {
        return state;
    }

    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<Requirements>> requirements() {
        return (Optional<List<Requirements>>) requirements;
    }

    public final static Builder builder() {
        return new Builder();
    }

    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
     */
    public RequirementSets withKey(String key) {
        Utils.checkNotNull(key, "key");
        this.key = Optional.ofNullable(key);
        return this;
    }

    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
     */
    public RequirementSets withKey(Optional<String> key) {
        Utils.checkNotNull(key, "key");
        this.key = key;
        return this;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
     */
    public RequirementSets withEffectiveFrom(String effectiveFrom) {
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        this.effectiveFrom = JsonNullable.of(effectiveFrom);
        return this;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
     */
    public RequirementSets withEffectiveFrom(JsonNullable<String> effectiveFrom) {
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        this.effectiveFrom = effectiveFrom;
        return this;
    }

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
     */
    public RequirementSets withState(String state) {
        Utils.checkNotNull(state, "state");
        this.state = Optional.ofNullable(state);
        return this;
    }

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
     */
    public RequirementSets withState(Optional<String> state) {
        Utils.checkNotNull(state, "state");
        this.state = state;
        return this;
    }

    public RequirementSets withRequirements(List<Requirements> requirements) {
        Utils.checkNotNull(requirements, "requirements");
        this.requirements = Optional.ofNullable(requirements);
        return this;
    }

    public RequirementSets withRequirements(Optional<? extends List<Requirements>> requirements) {
        Utils.checkNotNull(requirements, "requirements");
        this.requirements = requirements;
        return this;
    }
    
    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        RequirementSets other = (RequirementSets) o;
        return 
            Objects.deepEquals(this.key, other.key) &&
            Objects.deepEquals(this.effectiveFrom, other.effectiveFrom) &&
            Objects.deepEquals(this.state, other.state) &&
            Objects.deepEquals(this.requirements, other.requirements);
    }
    
    @Override
    public int hashCode() {
        return Objects.hash(
            key,
            effectiveFrom,
            state,
            requirements);
    }
    
    @Override
    public String toString() {
        return Utils.toString(RequirementSets.class,
                "key", key,
                "effectiveFrom", effectiveFrom,
                "state", state,
                "requirements", requirements);
    }
    
    public final static class Builder {
 
        private Optional<String> key = Optional.empty();
 
        private JsonNullable<String> effectiveFrom = JsonNullable.undefined();
 
        private Optional<String> state = Optional.empty();
 
        private Optional<? extends List<Requirements>> requirements = Optional.empty();  
        
        private Builder() {
          // force use of static builder() method
        }

        /**
         * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
         */
        public Builder key(String key) {
            Utils.checkNotNull(key, "key");
            this.key = Optional.ofNullable(key);
            return this;
        }

        /**
         * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with the same `key` and different `effective_from` values.
         */
        public Builder key(Optional<String> key) {
            Utils.checkNotNull(key, "key");
            this.key = key;
            return this;
        }

        /**
         * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
         */
        public Builder effectiveFrom(String effectiveFrom) {
            Utils.checkNotNull(effectiveFrom, "effectiveFrom");
            this.effectiveFrom = JsonNullable.of(effectiveFrom);
            return this;
        }

        /**
         * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are effective dated, while others are not. Multiple requirement sets for the same state/key can/will exist with unique effective dates. If a requirement set is has an `effective_from` value, all requirement sets with the same key will also have an `effective_from` value.
         */
        public Builder effectiveFrom(JsonNullable<String> effectiveFrom) {
            Utils.checkNotNull(effectiveFrom, "effectiveFrom");
            this.effectiveFrom = effectiveFrom;
            return this;
        }

        /**
         * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
         */
        public Builder state(String state) {
            Utils.checkNotNull(state, "state");
            this.state = Optional.ofNullable(state);
            return this;
        }

        /**
         * One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)
         */
        public Builder state(Optional<String> state) {
            Utils.checkNotNull(state, "state");
            this.state = state;
            return this;
        }

        public Builder requirements(List<Requirements> requirements) {
            Utils.checkNotNull(requirements, "requirements");
            this.requirements = Optional.ofNullable(requirements);
            return this;
        }

        public Builder requirements(Optional<? extends List<Requirements>> requirements) {
            Utils.checkNotNull(requirements, "requirements");
            this.requirements = requirements;
            return this;
        }
        
        public RequirementSets build() {
            return new RequirementSets(
                key,
                effectiveFrom,
                state,
                requirements);
        }
    }
}

